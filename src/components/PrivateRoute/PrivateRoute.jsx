// import React from 'react';
// import { useSelector } from 'react-redux';
// import { Navigate } from 'react-router-dom';
// import { selectAuthenticated } from 'Redux/user/authSelector';

// export const PrivateRoute = ({ children, redirectTo = '/contacts' }) => {
//   const authenticated = useSelector(selectAuthenticated);
//   return authenticated ? <Navigate to={redirectTo} replace /> : children;
// };

// example of the rpivate route, same funcionality implemented in App.jsx directly
